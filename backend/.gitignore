# Dependencies
node_modules/
node_modules

# Environment
.env
.env.local
.env.*.local
database.sqlite
generated-reports/
reports/output/
coverage/
*.log
# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Build output
dist/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?

# Scratch / temp dev scripts (not for repo)
scratch/

# Import module temp uploads (Integration 3, WP6) — multer-saved .xlsx files awaiting
# validate/confirm; deleted on success/failure/cancel by the app itself, this is just a
# backstop so a crashed process's leftovers never get committed.
var/tmp/imports/
# Superseded by var/tmp/imports/ above — kept here in case it reappears locally.
temp-imports/
