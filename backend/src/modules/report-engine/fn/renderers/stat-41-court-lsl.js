export function renderStat41(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_41') || workbook.getWorksheet('STAT 41');
  if (!ws) return;
  // Court disposal of L&SL cases not available in system — template preserved.
}
