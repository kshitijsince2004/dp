export function renderStat21(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_21') || workbook.getWorksheet('STAT 21');
  if (!ws) return;
  // Kalandra disposal data not tracked — template preserved.
}
