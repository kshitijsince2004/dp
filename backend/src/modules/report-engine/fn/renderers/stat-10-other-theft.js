export function renderStat10(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_10') || workbook.getWorksheet('STAT 10');
  if (!ws) return;
  // Other theft worked-out sub-category detail not tracked — template preserved.
}
