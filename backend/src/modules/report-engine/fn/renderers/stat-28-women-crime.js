export function renderStat28(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_28') || workbook.getWorksheet('STAT 28');
  if (!ws) return;
  // Crime Against Children under BNS — child victim flag not captured — template preserved.
}
