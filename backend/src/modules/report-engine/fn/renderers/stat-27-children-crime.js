export function renderStat27(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_27') || workbook.getWorksheet('STAT 27');
  if (!ws) return;
  // Juvenile Justice Act child-victim breakdown not separately tracked — template preserved.
}
