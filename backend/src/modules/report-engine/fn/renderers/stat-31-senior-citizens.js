export function renderStat31(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_31') || workbook.getWorksheet('STAT 31');
  if (!ws) return;
  // North-East resident victim flag not captured in current records — template preserved.
}
