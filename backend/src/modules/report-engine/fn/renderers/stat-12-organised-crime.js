export function renderStat12(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_12') || workbook.getWorksheet('STAT 12');
  if (!ws) return;
  // Organised crime gang/syndicate data not tracked in system — template preserved.
}
