export function renderStat14(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_14') || workbook.getWorksheet('STAT 14');
  if (!ws) return;
  // Preventive measure detail not tracked in system — template preserved as-is.
}
