export function renderStat08(workbook, _scope, _calcData) {
  const ws = workbook.getWorksheet('STAT_8') || workbook.getWorksheet('STAT 8');
  if (!ws) return;
  // Other BNS worked-out sub-section detail not available — template preserved.
}
