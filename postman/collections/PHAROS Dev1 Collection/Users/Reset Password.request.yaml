$kind: http-request
url: "{{baseUrl}}/users/:id/reset-password"
method: POST
pathVariables:
  id: ""
body:
  type: json
  content: |-
    {
      "newPassword": "adminreset123"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 6000
