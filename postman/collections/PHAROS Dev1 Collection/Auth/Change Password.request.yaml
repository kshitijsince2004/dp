$kind: http-request
url: "{{baseUrl}}/auth/change-password"
method: PUT
body:
  type: json
  content: |-
    {
      "oldPassword": "test123",
      "newPassword": "newpassword123"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
